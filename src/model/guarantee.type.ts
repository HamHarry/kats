import { Product } from "./product.type";

export interface Guarantees {
  number: string;
  volume: string;
  bookDate: string;
  bookTime: string;
  name: string;
  carType: string;
  carModel: string;
  register: string;
  product: Product;
  price: string;
  tel: string;
  image: string;
}
